# Optional — not wired yet. Copy to .env for local experiments.
# Production today uses constants in supabase-client.js

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Future: server-side only (never expose in HTML/JS)
# SUPABASE_SERVICE_ROLE_KEY=

# Future: replace client-side admin gate
# ADMIN_PASSWORD=
